# 研究设计待补项（方法链条闭合清单）

以下内容对应「情绪驱动 Black–Litterman」方法论述中仍缺或偏薄的部分，用于写作与答辩前自检。来源：方法缺口梳理（实证设计层面，非代码任务）。

---

## 1. 从情绪序列到 BL 的代数关系（最关键）

- [ ] 明确 SI 粒度：每只股票一条 / 市场单一指数 / 行业聚合等。
- [ ] 钉死 \(P\) 每一行对应的线性组合类型：绝对观点、相对观点（\(i\) 对 \(j\)）、组合观点等。
- [ ] 若 Q 为多维观点，说明如何从 SI 生成多条视图及是否稀疏。
- [ ] **时间对齐**：发帖时刻、收盘、持有期收益起止；避免「收盘后情绪」解释「同日收益」的前视偏差。

---

## 2. \(\Omega\)（观点置信度）的可操作定义

- [ ] 说明 \(\Omega\) 结构（对角 vs 非对角）及选择理由。
- [ ] 给出「历史情绪—未来收益」相关到方差的**显式映射**（是否与 \(\tau\)、\(P\Sigma P'\) 成比例等）。
- [ ] 分资产/分时期估计时的规则；样本短或相关不稳定时的正则化或上下界。

---

## 3. 情绪模型（BERT-BiLSTM）的训练与泛化

- [ ] 标注数据来源、标签定义（离散/连续）、训练/验证划分。
- [ ] 股吧语言域适应：是否微调、样本外时段是否滚动再训练。
- [ ] 报告效果指标（准确率/F1 等）或与基线（如词典法）对比。

---

## 4. 聚合为 SI 的显式公式与稳健性

- [ ] 时间、点赞、用户等级等加权的**具体函数形式**（线性/对数/半衰期等）。
- [ ] 缺失与异常处理：删帖、刷赞、重复帖、水军。
- [ ] 稳健性：替换加权方案后结论是否仍成立（敏感性分析）。

---

## 5. 回测与优化的同一套规则

- [ ] 均值-CVaR：置信水平 \(\alpha\)、换手约束、持仓上下限、是否做空等与**所有对照组一致**。
- [ ] 交易成本、滑点、涨跌停、停牌是否纳入；若不纳入，写明局限。
- [ ] **滚动 6 个月**分别用于哪些量（\(\Sigma\)、\(\tau\)、情绪—收益映射等）；窗口重叠方式；每月调仓时**用哪段数据更新哪些参数**。

---

## 6. 对照实验的公平性

- [ ] 同一投资域、同一再平衡日历、同一约束与成本。
- [ ] 情绪版与「历史均值观点」版 BL 的 **\(P\) 维度与观点条数**是否匹配；若不匹配，说明或重新设计对照。

---

## 7. 统计检验的对象与假设

- [ ] 配对 t 检验作用于何种频率的收益；是否考虑自相关（如 Newey-West、bootstrap）。
- [ ] 多指标/多子样本时是否考虑多重比较。
- [ ] 同时报告经济显著性与统计显著性。

---

## 8. 合规、伦理与可复现性

- [ ] 合规：平台条款/robots、仅公开信息、匿名化、研究用途边界；数据二次使用说明。
- [ ] 可复现：关键参数表（\(\tau\)、\(\Omega\) 规则、CVaR \(\alpha\)、窗口长度等）；附录或补充材料中的流程与版本说明。

---

## 小结（闭合标准）

方法链条闭合需满足：**\(P\)、\(Q\) 与 SI 的严格对应与时间对齐**；**\(\Omega\) 的显式公式与估计细节**；**情绪模型训练/评估与 SI 聚合的可复现描述**；**回测与对照组在约束与成本上的可比性**；**统计推断设定清晰**。

---

## 附录：与当前仓库代码的对照（工程自检）

> 上文 §1–§8 主要为**研究设计与论文方法论**待补项（见文件开头「非代码任务」）。下表区分「论文里要写的」与「代码里已有/仍缺」——**不要求**为闭合 §1–8 而对全仓库做一次「大重构」；优先补文档、补实验设计，再按需增量改代码。

| 章节 | 代码侧大致覆盖 | 与 Todo 相比的主要缺口 |
|------|----------------|------------------------|
| §1 情绪→BL、\(P/Q\)、时间对齐 | `emotion_bl` 管线、`news_weekly` 周线键与行情 `Date` 对齐、`get_post_weight_with_sentiment_views` | SI 粒度未在配置中形式化；**发帖 vs 收市**的前视/事件研究规则未编码；\(P\) 每行「绝对/相对」语义未在配置层钉死 |
| §2 \(\Omega\) | 对角 \(\Omega\)、规则/批次内缩放（dashboard） | 缺论文级「情绪—收益→方差」显式映射；**非对角** \(\Omega\) 未支持 |
| §3 情绪模型训练与泛化 | `SENTIMENT_BACKEND`：snownlp / bert / llm | **无** BERT-BiLSTM **训练**与数据划分、无系统评测报表（与论文 §3 不对齐属预期） |
| §4 SI 聚合与稳健性 | 关键词 + 可选 LLM 映射 ticker | 无点赞/等级等加权；**水军/删帖**未建模；稳健性实验未自动化 |
| §5 回测与优化同一规则 | `code/back_test.py`（`get_post_weight` + 等权）；周频 Agent 走情感视图 | **无**均值-CVaR、换手约束、滑点/停牌统一模块；`BackTest` 与 `run_weekly_blm_agent` **路径不一致**；「滚动 6 月」未实现 |
| §6 对照实验公平性 | 可无新闻周（零观点）vs 有新闻 | 未自动化保证情绪版与基线 **\(P\) 维度/条数** 匹配的对照套件 |
| §7 统计检验 | — | **未实现** t 检验、Newey-West、bootstrap、多重比较校正 |
| §8 合规与可复现 | `.env`、JSONL、脚本与 API | 缺「一键导出参数表/版本附录」；各数据源 ToS 说明主要在工程外文档 |

**代码结构备注**：存在两套 BL 相关实现——`code/black_litterman.py`（读数 + 回测闭环）与 `emotion_bl/bl/black_litterman.py`（矩阵引擎）。适合**用 README/架构图说明边界**，按需抽取公共层；不宜为「对齐 Todo」而盲目全库重构。
